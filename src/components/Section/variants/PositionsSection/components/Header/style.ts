import styled from "styled-components";

export const StyledHeader = styled.div`
  padding: 1rem;
  margin-bottom: 0.25rem;
  margin-top: 0.25rem;
  display: flex;
`

export const StyledDate = styled.div`
  color: #111827;
  line-height: 1.5rem;
  font-weight: 500;
`

export const StyledDifference = styled.div`
  margin-left: auto;
  line-height: 1.5rem;
  font-weight: 500;
  color: #d1d5db;
`