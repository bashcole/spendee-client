
import { Breakpoints, Colors, MediaQueries, Shadows, Spacing } from "./types";

export interface SpendeeTheme {
    siteWidth: number;
    isDark: boolean;
    colors: Colors;
    breakpoints: Breakpoints;
    mediaQueries: MediaQueries;
    spacing: Spacing;
    shadows: Shadows;
}
